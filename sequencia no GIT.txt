PRIMEIRO LIMPAR O CONTEUDO DO .GITIGNORE

cd "C:\api-sqlserver"

git add dbconfigs.json

git commit -m "subindo dbconfigs.json atualizado"

git push

git rm --cached dbconfigs.json

echo "dbconfigs.json" >> .gitignore

git add .gitignore

git commit -m "removendo dbconfig.json"

git push
